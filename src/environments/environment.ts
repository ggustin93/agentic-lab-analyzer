/**
 * Development environment. Replaced by environment.prod.ts in production
 * builds via angular.json fileReplacements.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000/api/v1'
};
